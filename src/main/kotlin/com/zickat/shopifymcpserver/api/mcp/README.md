# Réservé — LOT0-08

Dossier posé vide par `LOT0-02` (scaffolding). Les outils MCP (Spring AI `@McpTool`) arrivent en
`LOT0-08`, après que le catalogue (lot 2+) et le resource server OAuth (`LOT0-05`) existent.

Ce fichier n'est pas un module Kotlin (aucune classe dedans) : tant qu'il n'y a aucun `.kt` ici,
Spring Modulith ne le compte pas comme un module et `ApplicationModules.verify()` l'ignore. Il
disparaît naturellement de ce README le jour où `LOT0-08` y écrit du code.
