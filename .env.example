# Connexion MongoDB (backend.md — mongodb-driver-kotlin-sync + Spring Data MongoDB).
# Les autres variables (clé maîtresse du coffre, config de l'IdP OAuth, etc.) seront ajoutées par
# les tâches qui les introduisent (LOT0-04, LOT0-05) — pas retro-ajoutées en bloc ici.
MONGODB_URI=mongodb://localhost:27017/shopify_mcp_server
